# Application database connection (restricted user, subject to RLS)
DATABASE_URL="postgresql://nubase_app:nubase_app@localhost:5435/nubase"

# Admin database connection (superuser, for migrations and seeding)
DATABASE_URL_ADMIN="postgresql://nubase:nubase@localhost:5435/nubase"

# Debug authentication token for curl/API testing
# Usage: curl -H "Authorization: Bearer debug:<userId>:<secret>" http://tavern.localhost:4001/tickets
DEBUG_AUTH_TOKEN="dev-secret-123"

PORT=4001
DB_PORT=5435
NODE_ENV=test