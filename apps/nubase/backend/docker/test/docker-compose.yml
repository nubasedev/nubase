services:
  postgresql:
    container_name: nubase-db-test
    image: "postgres:17.5"
    labels:
      com.nubase.managed: "true"
      com.nubase.project: "nubase"
      com.nubase.env: "test"
    volumes:
      - ./postgresql-data:/var/lib/postgresql/data
      - ./postgresql-init:/docker-entrypoint-initdb.d
    ports:
      - "5435:5432"
    environment:
      - POSTGRES_DB=nubase
      - POSTGRES_USER=nubase
      - POSTGRES_PASSWORD=nubase
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nubase -d nubase"]
      interval: 5s
      timeout: 5s
      retries: 5
